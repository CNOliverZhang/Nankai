<view class="loading {{showLoad ? 'show' : 'hidden'}}">
  <view class='loading-text'>正在加载中</view>
  <view class='loading-circle'>LOADING</view>
</view>
<view class='texts'>
  <view class='{{item.style}}' wx:for="{{words}}" wx:key="text">{{item.text}}</view>
</view>
<button bindtap='next' class='{{startButtonStyle}}' hover-class='show bar-button__hover'>追忆先贤</button>